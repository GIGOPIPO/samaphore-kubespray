---
- hosts: all
  become: true

  tasks:
    - name: Install open-iscsi
      package:
        name: open-iscsi
        state: present

    - name: Enable iscsid
      systemd:
        name: iscsid
        enabled: yes
        state: started
